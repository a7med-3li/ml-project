"""src/models/__init__.py

Package for additional model implementations (e.g., Ridge, Lasso, tree-based models).

Place modules here (ridge.py, tree.py) and import them from `src.models` when needed.
"""

# This file intentionally left minimal to mark the package; add implementations as needed.
