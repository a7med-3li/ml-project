"""scripts/train_linear.py

This is the canonical CLI entrypoint for the numerical training pipeline.

What this file will do (placeholder):
- Parse CLI arguments: --data-path, --target, --output-dir, --test-size, --random-seed
- Load the CSV using `src.data_loader.load_csv`
- Perform basic cleaning and preprocessing (imputation, encoding)
- Split train/test, call `src.linear_model.train_linear_regression`
- Evaluate and call `src.linear_model.save_model`

Currently this file is a placeholder with usage notes. Implement the runnable script here when ready.
"""

if __name__ == "__main__":
    # Placeholder entrypoint. Replace this block with argparse-driven orchestration.
    print("scripts/train_linear.py is a placeholder. Implement CLI orchestration here.")
